@model IEnumerable<Online_Bus_Reservation_System.Areas.Admin.Models.Bus>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BusType1.TypeId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BusType1.TypeId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BusNo }) |
            @Html.ActionLink("Details", "Details", new { id=item.BusNo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BusNo })
        </td>
    </tr>
}

</table>
