@model IEnumerable<Online_Bus_Reservation_System.Areas.Admin.Models.City>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CityName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CityName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CitiesId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CitiesId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CitiesId })
        </td>
    </tr>
}

</table>
